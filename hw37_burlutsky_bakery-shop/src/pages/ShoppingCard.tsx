import {Container, Typography} from '@mui/material';

export default function ShoppingCard() {
    return (
        <Container sx={{py: 3}}>
            <Typography variant="h4" gutterBottom>src/pages/ShoppingCard.tsx</Typography>
            <Typography>Authorize access</Typography>
        </Container>
    );
}
